name: Deploy to Aruba Hosting

on:
  push:
    branches:
      - main  # Change to your default branch if different

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'  # Adjust to your Node version
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build SvelteKit site
        run: npm run build
        env:
          # Add any build-time environment variables here
          PUBLIC_API_URL: ${{ secrets.PUBLIC_API_URL }}
      
      # - name: Deploy to Aruba via FTP
      #   uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      #   with:
      #     server: ${{ secrets.FTP_SERVER }}
      #     username: ${{ secrets.FTP_USERNAME }}
      #     password: ${{ secrets.FTP_PASSWORD }}
      #     local-dir: ./build/  # SvelteKit adapter-static default output
      #     server-dir: ./  # Root or subdirectory on Aruba (e.g., ./public_html/)
      #     exclude: |
      #       **/.git*
      #       **/.git*/**
      #       **/node_modules/**


      # - name: Deploy to Aruba via SFTP
      #   uses: wlixcc/SFTP-Deploy-Action@v1.2.4
      #   with:
      #     server: ${{ secrets.SFTP_SERVER }}
      #     username: ${{ secrets.SFTP_USERNAME }}
      #     password: ${{ secrets.SFTP_PASSWORD }}
      #     port: 22
      #     local_path: './build/*'
      #     remote_path: '/public_html/'
      #     sftp_only: true